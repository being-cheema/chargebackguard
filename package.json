{
  "name": "chargebackguard",
  "private": true,
  "scripts": {
    "dev": "concurrently -n backend,frontend -c blue,green \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "seed": "npm run seed:full --prefix backend",
    "evaluate": "npm run evaluate --prefix backend",
    "test": "npm test --prefix backend",
    "evaluate:integrity": "npm run evaluate:integrity --prefix backend",
    "build": "npm run build --prefix backend && npm run build --prefix frontend"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
